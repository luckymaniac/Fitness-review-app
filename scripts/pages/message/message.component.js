import template from './message.html';
import controller from './message.controller';
import './message.scss';

let component = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default component;
